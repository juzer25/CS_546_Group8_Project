<div class="container">
		<div style="width:84%;float:left">
 		<ul>
 			<li><a href="/">HOME</a></li>
            {{#if userName}} 
				
				{{#if isAdmin}}
				<li><a href="/broadband/newPlan">Add Broadband service</a></li>
    			
    			<li><a href="/allappointments">Check user Requests</a></li>
				{{else}}
                <li><a href="/appointment">CREATE APPOINTMENT</a></li>
				{{/if}}

            {{/if}}
			<li><a href="/broadband/broadbandPlans">PLANS</a></li>
            {{#if userName}}
			{{#if isAdmin}}
			{{else}}
			<li><a href="/checkout/yourorders">YOUR ORDER</a></li>
			{{/if}}
		    {{/if}}
		    <li><a href="/broadband/contactus">CONTACT US</a></li>
 		</ul>		
 		</div>
 		<div style="width: 16%;float:right"> 
        
 		<ul>	
        {{#if userName}}      
		    <li><a href="/users/profile">PROFILE</a></li>
		    <li><a href="/users/logout">LOGOUT</a></li>
        {{else}}
            <li><a href="/users/signup">REGISTER</a></li>
		    <li><a href="/users/login">LOGIN</a></li>
        {{/if}}
 		</ul>
 		</div>
</div>

{{#if userName}}
<table>
 
<tr>
			<th>Order Id</th> 
            <th>Plan name</th>
            <th>Plan Id</th>
            <th>Start Date</th>
            <th>End Date</th>
             <th>Price</th></tr>
{{#each listplanDetails}}
		<tr>
			<td>{{orderId}}</td> 
            <td>{{planName}}</td>
            <td>{{broadbandPlanId}}</td>
            <td>{{startDate}}</td>
            <td>{{endDate}}</td>
             <td>{{price}}$</td>
             <form>
            <td><button id="reg" type="submit" class="update" formmethod="post" formaction="/checkout/bill/{{broadbandPlanId}}" >View Bill</button></td>
         </form>            
        </tr>
        {{/each}}



</table>
{{else}}
<h1> You don't have any order history <h1>
{{/if}}