<script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44="
        crossorigin="anonymous"></script>
<div class="container">
		<div id="interior">
 		<ul>
 			<li><a href="/">HOME</a></li>
             {{!-- Don't think we need this link if we are already on that page --}}
             <li><a href="/broadband/broadbandPlans">PLANS</a></li>
		    <li><a href="/broadband/contactus">CONTACT US</a></li>
 		</ul>	
 		</div>
 		<div id="interior2"> 
        {{!-- {{#if userName}}
            <h2 class="userType">{{userName}}</h2>
        {{else}}
        <h2 class="userType" hidden>guest</h2> --}}
 		<ul>	
        {{#if userName}}      
		    <li><a href="/users/profile">PROFILE</a></li>
		    <li><a href="/users/logout">LOGOUT</a></li>
        {{else}}
            <li><a href="/users/signup">REGISTER</a></li>
		    <li><a href="/users/login">LOGIN</a></li>
        {{/if}}
 		</ul>
         {{!-- {{/if}}	 --}}
 		</div>
</div>

<a href="/broadband/statistics" target="_blank">Check Statistics</a>
<h1 class="table_heading">Broadband Plans</h1>
<div>
    {{#if noPlan}}
   
            <p>{{noPlan}}</p>
            
        {{/if}}
	<table id='plansTable' border="0">
        <tr>
            <th colspan="3">Plan Name</th>
            <th colspan="3">Cost</th>
            <th colspan="3">Validity</th>
            <th colspan="3">Limit</th>
            <th colspan="3">Discount</th>
            <th colspan="3">Statistics</th>
            <th colspan="3"></th>
        </tr>
 {{#if isAdmin}}
        {{#each broadbandList}}
        <tr>
            <td colspan="3"><p id='planName'>{{this.planName}}</p></td>
            <td colspan="3"><p id='price'>{{this.price}}</p></td>
            <td colspan="3"><p id='validity'>{{this.validity}} Months</p></td>
            <td colspan="3"><p id='limit'>{{this.limit}}</p></td>
            <td colspan="3"><p id='discount'>{{this.discount}}%</p></td>
            <td colspan="3"><p id='ststs'>{{this.userID.length}} Users</p></td>          
            {{!-- <td colspan="3"><button id="reg" type="submit" class="select" >Subscribe</button></td> --}}
            {{!-- {{#if isAdmin}} --}}
           
            
         <form>
            <td colspan="3"><button id="reg" type="submit" class="delete" formmethod="post" formaction="/broadband/scrap/{{this._id}}">Delete</button></td>
            <td colspan="3"><button id="reg" type="submit" class="update" formmethod="post" formaction="/broadband/getByName/{{this._id}}" >Update</button></td>
            {{!-- <a href="/broadband/getByName/{{this._id}}">Update</a> --}}
         </form></tr>
         {{/each}}
         {{else}}
         {{#each broadbandList}}
        <tr>
            <td colspan="3"><p id='planName'>{{this.planName}}</p></td>
            <td colspan="3"><p id='price'>{{this.price}}</p></td>
            <td colspan="3"><p id='validity'>{{this.validity}} Months</p></td>
            <td colspan="3"><p id='limit'>{{this.limit}}</p></td>
            <td colspan="3"><p id='discount'>{{this.discount}}%</p></td>
            <td colspan="3"><p id='ststs'>{{this.userID.length}} Users</p></td>          
            <td colspan="3"><button id="reg" type="submit" class="select" >Subscribe</button></td>
        </tr>{{/each}}
         {{/if}}
        
        
        {{#if isAdmin}}
        <tr>
        <form>
        <td colspan="28"><button id="reg" type="submit" class="add" formmethod="get" formaction="/broadband/newPlan" >Add New Plan</button></td>
        </form>
        </tr>
        {{/if}}
     </table>
</div>
<script src="/public/js/broadbandScript.js"></script>
