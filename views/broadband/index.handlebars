<h1>Welcome to BroadBand Website!!!</h1>
{{#if userName}}
    {{#if isAdmin}}
    <h2>{{userName}}</h2>
    <a>Add Broadband service</a>
    <a>Update BroadBand service</a>
    <a>Delete Broadband service</a>
    <a>Check user Requests</a>
    <a href="users/logout">Logout</a>
    {{else}}
    <h2>{{userName}}</h2>
    <a href="users/profile">Profile</a>
    <a href="users/logout">Logout</a>
    {{/if}}
{{else}}
    <a href="users/signup">Need to register? Click here to sign-up</a><br>  
    <br>
    <a href="users/login">Already have an account? Click here to log-in</a>
{{/if}}